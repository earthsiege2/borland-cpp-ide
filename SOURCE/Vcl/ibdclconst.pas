unit IBDCLConst;

interface

resourcestring
  SBackupCaption = 'Backup Database files';
  SRestoreSize = 'Size(Bytes)';
  SFileNames = 'Filename(s)';
  SPages = 'Pages';
  SDatabasefiles = 'Database Files|*.gdb';
implementation

end.
